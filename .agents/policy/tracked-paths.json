{
  "version": 1,
  "doc": "docs/tracked-path-guard.md",
  "checker": ".agents/scripts/tracked_path_check.py",
  "why": "A tracked Markdown, YAML, JSON, or TOML file must not name an in-tree path that does not exist. After the multi-repo split, routing docs that still point at `.remote-dev/state/` or a deleted coordinator tree are drift, not history.",
  "format_note": "JSON, not YAML, so the guard stays stdlib-only and can run in every existing CI job without a pip install step.",
  "scan": {
    "include_suffixes": [
      ".md",
      ".yaml",
      ".yml",
      ".json",
      ".toml"
    ],
    "include_suffixes_why": "Tracked documentation and configuration can route readers to missing files.",
    "skip_roots": [
      "vllm",
      "vllm-ascend"
    ],
    "skip_roots_why": "Volatile upstream submodules; not ours to police.",
    "fixture_paths": [
      ".agents/tests/test_tracked_path_check.py"
    ],
    "fixture_paths_why": "This guard's own tests must name dead paths as fixtures to prove detection. Nothing else belongs here: a file that genuinely needs an exemption needs a baseline row or a skip_paths entry with a why.",
    "skip_paths": [
      {
        "path": ".agents/policy/tracked-paths.json",
        "why": "This policy names scan roots and allow prefixes; those strings are not claims the prefixes exist as files."
      },
      {
        "path": ".agents/policy/tracked-paths-baseline.json",
        "why": "The baseline must repeat the dead token it is accepting; scanning it would make every row a new violation."
      },
      {
        "path": ".agents/skills/ascend-profiling-analysis/scripts/ascend_profile/knowledge/known_counterexamples.md",
        "why": "Historical counterexamples in the profiling-analysis skill; excluded from this review and retained unchanged."
      }
    ],
    "skip_paths_why": "A handful of tracked policy/fixture files must name departed or external paths as data. They are listed here with a why, not hidden in the fixture exemption."
  },
  "token_prefixes": [
    ".agents/",
    ".remote-dev/",
    ".claude/",
    ".codex/",
    ".cursor/",
    ".grok/",
    ".github/",
    "docs/",
    "scripts/"
  ],
  "token_prefixes_why": "These are the in-repo roots an agent is told to open. A token must start with one of them and contain at least one slash, so a prose mention of `docs` or `scripts` is not a path.",
  "allow_prefixes": [
    {
      "prefix": ".vaws-local/",
      "why": "Untracked runtime state; legitimately absent from the tracked tree."
    },
    {
      "prefix": ".vaws-runtime/",
      "why": "Untracked runtime state; legitimately absent from the tracked tree."
    },
    {
      "prefix": "<",
      "why": "Placeholder tokens such as <session-id> or <vaws-top>/scripts/vaws-top.py are not real paths."
    }
  ],
  "allow_prefixes_why": ".remote-dev/state/ is deliberately absent: that path is post-migration drift and must fail. Placeholders and untracked state roots are the only silent exceptions.",
  "rules": [
    {
      "id": "missing-path",
      "kind": "missing-in-tree-path",
      "severity": "error",
      "title": "Tracked docs must not name a missing in-tree path",
      "why": "A reader who follows a `.agents/`, `docs/`, or `.remote-dev/` path that is not in this tree is being routed at the pre-migration layout."
    }
  ]
}
